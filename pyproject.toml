[project]
name = "beta-earth-sovereignty-next"
version = "0.51.1"
description = "Local-first browser RPG with tactical combat, faction choices, and resilient saves"
requires-python = ">=3.11,<3.14"
dependencies = []
authors = [{name = "Gateway Information Group LLC"}]

[tool.unittest]
start-directory = "tests"
